import 'package:flutter/material.dart';

class AppColors {

  AppColors._(); // costruttore privato così non devo instanziarla nell'app

  static const background = Color(0xFFFFFFFF);
  static const buttonEnabled = Color(0xFF373737);
  static const fieldText = Color(0xCC373737);
  static const iconFocused = Color(0xFF373737);
  static const iconUnfocused = Color(0xFFFFFFFF);
  static const searchBar = Color(0x7FDDC4CA);
  static const tapBarBackground = Color(0xFFF87594);
  static const textPrimary = Color(0xFF373737);

}